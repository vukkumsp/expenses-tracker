import SQLite, { SQLiteDatabase } from "expo-sqlite";

// const db = SQLite.openDatabaseSync("test.db");

// export const readAllData = (tableName: string) => {
//     const statement = db.prepareSync("SELECT * FROM $value");
//     return statement.executeSync({$value: tableName});
// }